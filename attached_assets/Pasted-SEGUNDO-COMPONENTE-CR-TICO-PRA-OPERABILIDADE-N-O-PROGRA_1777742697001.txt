SEGUNDO COMPONENTE CRÍTICO PRA OPERABILIDADE NÃO-PROGRAMADOR. Sem JSON em lugar nenhum.

/app/admin/meta/page.tsx (admin-protected): lista snapshots de meta_snapshots com scope='global' do game_id atual. Colunas: version, criado_em, ativa, n_arquetipos (length de archetypes), análises_geradas, ações (Ver / Duplicar / Ativar).

Botão "Criar nova snapshot": abre /admin/meta/new, copia conteúdo da snapshot ativa atual como ponto de partida.

/app/admin/meta/[id]/page.tsx: lista os arquetipos da snapshot como cards visuais grandes com nome, tier badge, WR, share. Cada card tem botão "editar" e "duplicar". Botão "+ Adicionar arquetipo" no topo.

/app/admin/meta/[id]/archetype/[archIdx]/edit: formulário estruturado:
- Nome (text required)
- Nome PT-BR (text optional)
- Tier (dropdown: S, A, B, C)
- Win rate % (number 0-100)
- Meta share % (number 0-100)
- Cores (multi-select de chips coloridas — opções configuráveis por jogo: Digimon = red/blue/yellow/green/black/purple/white)
- Play style (dropdown: aggro, midrange, control, combo, toolbox, midrange-toolbox, aggro-combo, etc) + textarea "Notas em PT" (play_style_pt)
- Coach notes PT (textarea grande)

Bloco "Cartas-chave":
- Lista atual com cada carta: imagem mini (da API), nome, código, dropdown qty, dropdown role (engine/finisher/tempo/control/removal/tamer/option/egg), input note_pt
- Botão "+ Adicionar carta": abre busca com autocomplete. Conforme digita, chama API digimoncard.io e mostra resultados com imagem mini. Click adiciona à lista.
- Drag-to-reorder (use @dnd-kit ou similar)

Bloco "Decklist exemplar — Main Deck":
- Mesma busca de carta + qty
- Validação client-side em tempo real: soma das qty deve ser exatamente 50 (ou main_deck_size do jogo). Banner vermelho se não bater.

Bloco "Decklist exemplar — Egg Deck":
- Mesma UI. Validação: 0 a 5.

Bloco "Matchups":
- Dropdown de outros arquetipos da snapshot + input WR % + botão "adicionar"
- Lista com cada matchup: nome do oponente, WR. Separa visualmente em "matchups bons" (WR ≥ 50%) e "ruins" (< 50%) automaticamente.

Botão "Salvar arquetipo" volta pra lista de arquetipos da snapshot.

Voltando em /admin/meta/[id]: botão "Salvar snapshot" pede campo notes, cria nova row em meta_snapshots com active=false. Botão "Ativar esta snapshot" igual ao H6.10.

Sem JSON visível em nenhum momento da UX. Validação client-side gentil — mensagens em PT explicando o que tá errado.