ESTE PROMPT É O CORAÇÃO DA OPERABILIDADE PRO MEU FILHO NÃO-PROGRAMADOR. Implemente com cuidado de UX.

/app/admin/prompts/page.tsx (admin-protected): lista todas as versões em tabela com colunas: version, criado_em, ativa (badge verde se sim), análises_geradas (COUNT analyses), feedback_pos_pct, criado_por (placeholder admin), ações (Ver / Editar / Ativar / Duplicar).

Botão "Criar nova versão" no topo abre /admin/prompts/new.

/app/admin/prompts/new e /admin/prompts/[id]/edit: layout 2 colunas:
- Esquerda (60%): markdown editor — use @uiw/react-md-editor ou similar. Fonte mono, syntax highlighting básico. Toolbar mínima.
- Direita (40%): preview renderizado em tempo real do markdown.

Acima do editor: campo "version" (text, required, sugestão auto-incremental tipo "v2", "v3"). Campo "notes" (text, placeholder "o que mudou nessa versão").

Painel lateral colapsável "Variáveis disponíveis" listando:
- {{game_name}} — Nome do jogo (ex: "Digimon Card Game")
- {{game_card_code_pattern}} — Padrão de código de carta
- {{game_card_code_examples}} — Exemplos de códigos
- {{game_deck_rules}} — Regras de construção do deck
- {{archetypes_context}} — Lista dos arquetipos do meta atual
Cada uma com botão "inserir aqui" que cola a variável na posição do cursor.

Botão "Testar com deck salvo" abre modal: lista 10 análises recentes (escolhe uma como teste), clica → backend roda o prompt RASCUNHO (não o ativo) com aquele deck via Anthropic API, mostra resposta side-by-side com a resposta original. Custo dessa chamada de teste vai pra api_costs com flag is_test=true (adicionar coluna).

Botão "Salvar como nova versão" (sempre cria nova, nunca edita versão existente — segurança contra perda de prompt funcional). Botão "Ativar esta versão" pede confirmação modal e faz transação: UPDATE prompts SET active=false WHERE game_id=$1; UPDATE prompts SET active=true WHERE id=$2.

Histórico mostra quem ativou (admin) e quando — adicionar coluna activated_at, activated_by (placeholder "admin" no MVP).

Usabilidade crítica: meu filho NÃO sabe TypeScript, NÃO sabe regex, NÃO precisa entender que existem variáveis de programação. Pra ele, são "campos" que o sistema preenche sozinho. UX deve refletir isso.